export default class Point {
    constructor(x, y, thickness, color, type) {
      this.x = x;
      this.y = y;
      this.thickness = thickness;
      this.color = color;
      this.type = type;
    }
  }